server:
  port: 8083

spring:
  application:
    name: enrollment-service
  data:
    mongodb:
      uri: ${MONGO_URI:mongodb://localhost:27017/cb_enrollments}

jwt:
  secret: ${JWT_SECRET:dev-only-change-me}

logging:
  level:
    in.cloudblitz.enrollment: DEBUG
