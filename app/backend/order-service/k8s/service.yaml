apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: novocart
  labels:
    app: order-service
spec:
  type: ClusterIP
  ports:
  - port: 8083
    targetPort: 8083
    protocol: TCP
    name: http
  selector:
    app: order-service

