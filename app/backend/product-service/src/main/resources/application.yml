server:
  port: 8082

spring:
  application:
    name: product-service
  data:
    mongodb:
      uri: ${MONGO_URI:mongodb://localhost:27017/novocart_products}

logging:
  level:
    com.novacart.product: DEBUG
